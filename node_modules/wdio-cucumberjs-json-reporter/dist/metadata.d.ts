import type { AppData, BrowserData, MetadataObject, cjson_metadata } from './types';
import type { DesiredCapabilitiesExtended, RunnerStatsExtended } from './types/wdio';
import { Capabilities } from '@wdio/types';
export declare class Metadata {
    determineMetadata(data: RunnerStatsExtended): MetadataObject;
    determineDeviceName(metadata: cjson_metadata, currentConfigCapabilities: WebDriver.DesiredCapabilities): string;
    determinePlatformName(metadata: cjson_metadata, currentCapabilities: WebDriver.DesiredCapabilities): string;
    determinePlatformVersion(metadata: cjson_metadata, currentCapabilities?: Capabilities.Capabilities): string;
    determineAppData(currentConfigCapabilities: DesiredCapabilitiesExtended, metadata: cjson_metadata): AppData;
    determineBrowserData(currentCapabilities: Capabilities.DesiredCapabilities, currentConfigCapabilities: WebDriver.DesiredCapabilities, metadata: cjson_metadata): BrowserData;
}
//# sourceMappingURL=metadata.d.ts.map